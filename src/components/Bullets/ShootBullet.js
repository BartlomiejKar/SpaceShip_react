import { Bullet } from "../Spaceship/spaceship.style"
export const ShootBullet = (x, y) => {

    const element = document.getElementsByName(Bullet);
    console.log(element);
    console.log(Bullet)

    // const div = document.createElement("div");
    // div.appendChild(Bullet.target);
    // div.style.left = `${x}px`;
    // div.style.top = `${y}px`

    return (
        <>

        </>
    )
}